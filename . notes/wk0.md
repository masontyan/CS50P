equal sign = means assignment from right to left, copys right to left so input goes into variable x
as our programs become longer, we have comments, notes to ourselves in our code, "#"
pseudocode is expressing your thoughts kind of like a check list
str "stir" string very succinct 
everything in parentheses are arguments
when we look at arguments in documentatoins, we will have parameters that show what we can put in 
*objects means the print fucntion can take in as many strings, 
sep=' '(separator)                                                                                        magically creates space between two different arguments in parentheses
end='\n' means new line \n (moving/creating new line of text) below 
postional parameters, first come first served
escape character n 
f means special string format in a special way

you shoudl count from zero in loops

